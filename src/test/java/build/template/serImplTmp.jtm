package com.bjike.ser.${package};

import org.springframework.stereotype.Service;
import com.bjike.ser.ServiceImpl;
import com.bjike.dto.${package}.${clazz}DTO;
import com.bjike.entity.${package}.${clazz};

/**
 * ${des}业务操作实现
 *
 * @Author:	[ ${author} ]
 * @Date: [  ${date(),dateFormat="yyyy-MM-dd HH:mm:ss"} ]
 * @Description: [ ${des} ]
 * @Version: [ v1.0.0 ]
 * @Copy: [com.bjike]
 */
 @Service
public class ${clazz}SerImpl extends ServiceImpl<${clazz}, ${clazz}DTO> implements ${clazz}Ser {
 
}


