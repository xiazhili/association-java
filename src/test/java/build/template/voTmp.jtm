package com.bjike.vo.${package};

import com.bjike.vo.BaseVO;


/**
 * ${des}
 *
 * @Author:	[ ${author} ]
 * @Date: [  ${date(),dateFormat="yyyy-MM-dd HH:mm:ss"} ]
 * @Description: [ ${des} ]
 * @Version: [ v1.0.0 ]
 * @Copy: [com.bjike]
 */
public class ${clazz}VO extends BaseVO{

<%
	for(m in list){
%>
    /**
     * ${m.annotation}
     */
 	private ${m.type} ${m.fieldName};
<%}%>

<%
	for(m in list){
%>
 	public ${m.type} get${m.swapCaseName} () {
		return ${m.fieldName};
	}
	public void set${m.swapCaseName} (${m.type} ${m.fieldName}) {
		this.${m.fieldName} =${m.fieldName};
	}

<%}%>


}
